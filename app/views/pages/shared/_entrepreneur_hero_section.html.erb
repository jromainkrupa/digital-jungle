  <div class="bg-<%= locales[:colors][:primary_background] %>"> 
    <div class="pt-10 mx-auto max-w-7xl px-4 sm:pt-12 sm:px-6 lg:pt-16">
      <div class="lg:grid lg:grid-cols-2 lg:gap-20">
        <div data-controller="type" class="sm:text-center md:max-w-2xl md:mx-auto md:my-auto  lg:text-left">
          <h1 class="mt-1 block text-4xl font-extrabold sm:text-5xl xl:text-5xl text-<%= locales[:colors][:hero_text]%>">
            <%= t("#{locales[:path]}.hero-section.title_html") %>
          </h1>
          <p class="mt-3 text-base sm:mt-5 sm:text-xl lg:text-lg xl:text-xl text-<%= locales[:colors][:primary_title] %>">
            <%= t("#{locales[:path]}.hero-section.tagline") %>
          </p>
          <div class="mt-8 sm:max-w-lg sm:mx-auto sm:text-center lg:text-left lg:mx-0">
            <div class="mt-5 sm:mt-8 sm:flex sm:justify-center lg:justify-start">
              <div class="">
                <%= link_to t("#{locales[:path]}.hero-section.cta-primary"), new_user_registration_path, class: "#{locales[:colors][:primary_button]} lg:mr-4 mr-0 lg:mb-0 mb-4" %>
              </div>
            </div>
          </div>
        </div>

        <%= render "pages/shared/#{locales[:hero_right_content]}" %>
      </div>
    </div>

    <div class="divide pt-12">
      <%= render_svg "waves_divider", styles: "" %>
    </div>

  </div>