<div class="container mx-auto my-8 px-4">
  <div class="max-w-3xl mx-auto">
    <div class="flex justify-between items-center mb-4">
      <h1 class="h3 text-gray-400">
        <%= link_to "Pitches", entrepreneur_project_pitches_path %> ><span
          class="text-aquamarine-500"
        >
          Pitch #<%= @pitch.id %></span
        >
      </h1>
      <%= link_to t("edit"), edit_entrepreneur_project_pitch_path, class: "btn btn-link"
      %>
    </div>

    <div class="bg-gray-800 rounded shadow p-8">
      <div class="mb-4">
        <h5 class="text-gray-400">title</h5>
        <p class="text-white"><%= @pitch.title %></p>
      </div>
      <div class="mb-4">
        <h5 class="text-gray-400">video</h5>
        <div>
          <% if @pitch.video.key %>
            <%= cl_video_tag(@pitch.video.key, :loop => true, :controls => true,
          :transformation => [
            :height=>600, :width=>360, :crop=>"pad",
            :quality=>70, :duration=>"10"])  %>

          <% else %>
            <%= link_to "upload une video", edit_entrepreneur_project_pitch_path, class: "btn btn-link"%>
          <% end %>
        </div>
      </div>
    </div>
  </div>
</div>
<div class="flex items-center justify-start text-center">
  <%= link_to "Voir les pitchs", entrepreneur_project_pitches_path(@project), class:
  'btn btn-primary ml-8' %>
</div>
<div class="flex items-center justify-start text-center">
  <%= link_to "Créer un pitch", new_entrepreneur_project_pitch_path(@project), class:
  'btn btn-primary ml-8' %>
</div>


  </div>
</div>